[package]
name = "zono"
version = "0.2.1"
edition = "2021"
description = "A tiny cross-platform todo list with GUI, written in Rust"
license = "MIT"

[dependencies]
iced = "=0.12.1"

[profile.release]
opt-level = 2
lto = true
codegen-units = 1
